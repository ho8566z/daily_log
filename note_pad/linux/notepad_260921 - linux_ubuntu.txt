20260921


## Postman(포스트맨) 이란?

- API를 개발하고 테스트할 때 사용하는 도구예요. 주로 웹/앱 개발자가 백엔드 API가 제대로 동작하는지 확인할 때 사용한다.

#### Postman(포스트맨)의 주요 기능

- API 요청 테스트: GET, POST, PUT, DELETE 같은 HTTP 요청을 직접 만들고 서버에 보내서 응답을 확인할 수 있다.
- 컬렉션(Collection): 관련된 API 요청들을 묶어서 저장하고 관리할 수 았다.
- 환경 변수(Environment): 개발/스테이징/운영 서버 등 환경별로 URL이나 토큰 값을 다르게 설정할 수 았다.
- 자동화 테스트: 요청 후 응답 값을 검증하는 스크립트 작성이 가능하다.
- 문서화: API 명세를 자동으로 문서 형태로 만들어 팀과 공유 가능하다.
- Mock Server: 실제 백엔드가 없어도 가짜 응답을 만들어 프론트엔드 개발 진행이 가능하다.

#### 왜 많이 쓰는지?

- 브라우저에서 API를 테스트하려면 코드를 짜야 하지만, 포스트맨은 GUI로 URL, 헤더, 바디 등을 입력해서 클릭 몇 번으로 요청을 보낼 수 있어서 편리하다.
- 개발 과정에서 프론트/백엔드 협업 시 "이 API가 이렇게 동작하는 게 맞나?"를 빠르게 확인하는 용도로 많이 사용된다.

---

!image.png

!image.png

#### 스프링부트에서 Swagger(springdoc-openapi)를 환경별로 다르게 설정하는 방법

| 구분 | 개발 환경(개발 중) | 운영 환경(배포 전) |
| --- | --- | --- |
| 목적 | API 명세 확인, 프론트/백엔드 협업, 직접 테스트 | 보안상 외부 노출 차단 |
| springdoc.api-docs.enabled | true | false |
| springdoc.swagger-ui.enabled | true | false |
| 설정 위치 | application-dev.yml | application-prod.yml |
| 접근 URL | /swagger-ui.html 정상 접근 가능 | 접근 시 404 또는 차단 |
| 활성화 프로필 | spring.profiles.active=dev | spring.profiles.active=prod |

```powershell
// 화면 없이도 API 를 눌러볼 수 있다 → /swagger-ui.html
implementation 'org.springdoc:springdoc-openapi-starter-webmvc-ui:2.8.5'
```

!image.png

## Swagger VS. Postman

| 구분 | Swagger (springdoc-openapi) |  | Postman |  |
| --- | --- | --- | --- | --- |
| 주 목적 | API 명세 문서화 및 자동 생성 |  | API 요청 테스트 및 디버깅 |  |
| 동작 방식 | 코드(어노테이션)를 분석해 자동으로 문서·UI 생성 |  | 개발자가 직접 요청을 만들어서 실행 |  |
| 적용 위치 | 서버 코드에 통합 (@Operation, @Parameter 등) |  | 서버와 별개로 독립 실행되는 클라이언트 앱 |  |
| 문서화 | 코드와 100% 동기화됨 (자동) |  | 수동으로 문서화하거나 컬렉션 export 필요 |  |
| 테스트 시나리오 | 단순 요청/응답 확인 정도 |  | 시나리오 테스트, 스크립트, 자동화, 환경별 변수 관리 등 강력 |  |
| 협업 | 백엔드가 만든 API를 프론트가 바로 확인 가능 |  | 팀 단위로 컬렉션 공유, CI/CD 연동 가능 |  |
| 환경 분리 | 프로필(dev/prod)로 활성화 여부 제어 |  | Environment 기능으로 여러 서버 전환 |  |
| 인증 테스트 | 제한적 (Bearer 토큰 정도만) |  | OAuth, API Key 등 다양한 인증 방식 시뮬레이션 |  |
| 팀 공유 대상 | 주로 API 소비자(프론트엔드, 외부 개발자)에게 공개 |  | 주로 개발팀 내부에서 테스트/디버깅용 |  |
| 자동화 | 거의 없음 (문서 생성이 주 역할) |  | 테스트 스크립트, 뉴먼(Newman)으로 CI 파이프라인 연동 가능 |  |

!image.png

!image.png

Postman 공식 다운로드 주소 : 

https://www.postman.com/downloads/

!image.png

!image.png

- Postman을 실행하기 전에, Linux에서 docker start인지, Intellij run인지 확인해야 한다.
- http://localhost:8080의 Postman 화면(부분)

!image.png

---

!image.png

!image.png

!image.png

#### 왜 Postman의 기본 메서드가 Get인지?

| 가장 자주 사용하는 동작 | ‘조회’가 api 호출 중, 압도적으로 많음 |
| --- | --- |
| 가장 안전한 동작 | 서버 데이터를 바꾸지 않는다.
실수해도 위험이 적다. |

!image.png

#### 주요 포트 번호

| 포트 | 프로토콜/용도 | 설명 |  |  |
| --- | --- | --- | --- | --- |
| 80 | HTTP | 일반 웹사이트(http://) 기본 포트 — URL에서 생략 가능 |  |  |
| 443 | HTTPS | 보안 웹사이트(https://) 기본 포트 — SSL/TLS 암호화 적용 |  |  |
| 8080 | HTTP(대체) | 개발용 서버가 흔히 쓰는 포트 — 스프링부트 기본값 |  |  |
| 3000 | HTTP(대체) | React, Node.js(Express) 개발 서버가 자주 쓰는 포트 |  |  |
| 5432 | PostgreSQL | PostgreSQL 데이터베이스 기본 포트 |  |  |
| 3306 | MySQL | MySQL / MariaDB 데이터베이스 기본 포트 |  |  |
| 27017 | MongoDB | MongoDB 데이터베이스 기본 포트 |  |  |
| 6379 | Redis | Redis 캐시 서버 기본 포트 |  |  |
| 22 | SSH | 원격 서버 접속(SSH) 기본 포트 |  |  |
| 21 | FTP | 파일 전송(FTP) 기본 포트 |  |  |
| 25 | SMTP | 이메일 전송 기본 포트 |  |  |

---

#### localhost vs 127.0.0.1

| 항목 | localhost | 127.0.0.1 |
| --- | --- | --- |
| 의미 | "내 컴퓨터 자신"을 가리키는 도메인 이름(호스트명) | "내 컴퓨터 자신"을 가리키는 실제 IP 주소 |
| 범주 | 루프백(loopback) 주소의 별칭 | 루프백(loopback) IP 주소 자체 |
| 동작 방식 | 운영체제의 hosts 파일에서 127.0.0.1로 변환(resolve)됨 | 네트워크 레이어에서 바로 "나 자신"으로 처리 |
| 차이 | 이름으로 부르는 것 | 숫자로 부르는 것 |

---

!image.png

#### URL(Uniform Resource Locator)의 조각별 의미

| 조각 | 뜻 | 간단한 설명 |
| --- | --- | --- |
| http:// | 통신 방식(프로토콜) | 웹에서 주고받는 약속 |
| localhost | 어느 컴큐터인지 | 지금 이 컴퓨터 |
| :8080 | 어느 프로그램(문)인지 | 스프링 서버 |
| /api/products | 그 내부의 무엇인지 | 상품이라는 자원 |

!image.png

!image.png

#### Postman 화면 보는법

| 화면 구분 | 무엇을 보는지 |
| --- | --- |
| 요청(위쪽) | 메서드, 주소, (필요하면)Body |
| 응답 (아래쪽) | 상태 코드, 걸린 시간, Body |

!image.png

- shop-project-5th 코드 한번 읽기

!image.png

!image.png

## Collection 이란?

- Postman에서 관련된 API 요청들을 하나의 폴더처럼 묶어서 관리하는 기능이다.

#### Collection의 핵심 개념

- 여러 개의 API 요청을 낱개로 흩어놓지 않고, 하나의 프로젝트나 기능 단위로 그룹핑해서 저장하는 것이다.

#### Collection이 왜 필요한지?

- 정리: 회원 관련 API, 게시글 관련 API 등을 폴더별로 나눠서 관리한다.
- 재사용: 매번 URL, 헤더, 인증 정보를 새로 입력하지 않고 저장된 요청을 클릭만 하면 실행할 수 있다.
- 공유: 컬렉션을 export(JSON 파일)해서 팀원에게 공유하거나 Postman 계정으로 팀 워크스페이스에 공유할 수 있다.
- 자동화: 컬렉션 전체를 순서대로 실행해서 테스트 시나리오를 자동으로 돌릴 수 있다. (Collection Runner)
- 문서화: 컬렉션 자체를 API 문서처럼 활용 가능하다.(Publish 기능)

---

!image.png

!image.png

!image.png

#### Collection VS. History

|  | Collection |  | History |  |
| --- | --- | --- | --- | --- |
| 저장 방식 | 직접 Save를 눌러야 한다. |  | Send할 때 마다 자동으로 기록한다. |  |
| 정리 상태 | 사용자가 이름을 붙여 정리한다. |  | 시간순으로 정렬된다. |  |
| 언제 쓰는지 | 반복해서 계속 사용할 요청을 저장·관리할 때 (API 테스트 스위트 구성, 팀과 공유, 문서화 등) |  | 방금 보낸 요청을 다시 찾거나 재사용할 때 (임시로 빠르게 재실행) |  |

!image.png

!image.png

## Collection 구조

```
Collection(최상위 폴더) —→ Folder(하위 폴더, 선택) —→ Request(요청)
```

!image.png

- 회원가입 성공

!image.png

!image.png

!image.png

!image.png

!image.png

!image.png

#### Params & Body

| 구분 | 쓰는 곳 | 예시 |
| --- | --- | --- |
| Params(쿼리 파라미터) | 주소 자체에 값을 붙인다. | 검색어 등 |
| Body(요청 본문) | 주소와 분리된 별도의 상자 | 회원가입 JSON 등 |

!image.png

#### Post를 2번 보내면, 2번 실행되는지?

| 같은 Request로 POST를 2번 보내면 | 결과 |
| --- | --- |
| 첫 번째 | 201 Created — 생성 |
| 두 번쨰(같은 데이터) | 400 — 이미 입력된 정보입니다. |

!image.png

#### Header 탭

- Body가 편지 내용이라면, Header는 편지 봉투의 부가적 정보이다.

| Header | 자동으로 붙은 값 | 뜻 |
| --- | --- | --- |
| Content-Type | apllication/json | 본문이 JSON 형식이라는 표시 |
| Content-Length | (자동계산) | 본문의 바이트 크기 |

!image.png

!image.png

#### 반복되는 주소를 ‘변수’로 만들기

- 반복되는 “http://localhost:8080”을 변수로 선언했다.

!image.png

---

!image.png

!image.png

!image.png

!image.png

!image.png

!image.png

## 오류 진단 순서

| 증상 | 진단 순서 | 해결 방안 |
| --- | --- | --- |
| Could not send request / ECONNREFUSED | Spring Boot 실행 여부 | 터미널에서 앱 설정 |
| 404 Not Found | 주소 철자와 경로 | ex) /api/products, /api/users 확인 |
| 405 Method Not Allowed | GET / POST 선택 | 컨트롤러에 맞는 메서드 선택 |
| 400 Bad Request | Response body의 message | JSON과 입력값 수정 |
| 415 Unsupported Media Type | Body 형식 Content-Type | raw → JSON 선택 |
| 500 Internal Server Error | Spring Boot 터미널 로그 | 첫 예외 원인부터 읽기 |
| Unresolved variable | 변수 이름-범위-저장 | 주소 변수의 철자와 Collection 확인 |
- 프로그래밍 기초 개념으로 이미 정리한 것에 추가할 것

!image.png

- ,(컴마)를 제외하고 Post를 보냈을 때, 에러(400번대)

!image.png

- 이메일 형식에서 @를 넣지 않아서, “이메일 형식이 아닙니다.” 메세지 출력하고 에러 발생

!image.png

- 데이터 타입을 JSON이 아닌, HTML로 받았을 때의 에러

!image.png

- Variables를 ‘baseURL2’에서 ’baseurl2’로 바꿨을 때의 에러

!image.png

- 프로그램을 실행 중인 포트번호 8080에서 실행 중이지 않은 8081로 바꿨을 때의 에러

!image.png

- GET방식의 포트를 POST방식으로 보냈을 때의 에러

!image.png

!image.png

## Environment(환경) 란?

- Postman에서 변수를 저장해서 같은 요청을 여러 상황(개발/스테이징/운영 서버 등)에서 값만 바꿔가며 재사용할 수 있게 해주는 기능이다.

#### 왜 쓰는지

- 요청을 만들 때마다 URL이나 토큰 같은 값을 일일이 고쳐 쓰는 대신, {{변수명}} 형태로 넣어두고 Environment에서 실제 값을 관리하는 방식이다.

| 구분 | 범위 |
| --- | --- |
| Collection 변수 | 해당 Collection 내부에서만 사용한다. |
| Environment(환경) | 여러 Collection에서 공유 가능하다. |

!image.png

!image.png

## 26.09.21 숙제

|  | 무엇 |  |  |  |  | 예상 결과 |  |  |  |  |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | GET /api/products?keyword=마우스 |  |  |  |  | 200, 이름에 ‘마우스’ 들어간 것만 |  |  |  |  |
| 2 | POST /api/products (상품 새로 등록) |  |  |  |  | 201, 새 id 자동 부여 |  |  |  |  |
| 3 | PUT /api/products/{id} (2번 상품 가격-재고 수정) |  |  |  |  | 200, 수정된 값으로 응답 |  |  |  |  |
| 4 | DELETE /api/products/{id} (2번 상품 삭제) |  |  |  |  | 204, 응답 본문 없음 |  |  |  |  |
| 5 | 삭제한 상품을 GET /api/products/{id} |  |  |  |  | 400, ‘상품을 찾을 수 없다’ |  |  |  |  |
| 6 | POST /api/users/login → GET /api/users/me |  |  |  |  | 로그인 성공 후 me는 200 |  |  |  |  |

!image.png