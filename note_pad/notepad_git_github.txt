git + github 활용법

# git을 통해 정리하고 싶은 '디렉토리'에서 "git bash"을 선택해 실행 시작할 것

------------------------------------------------------------------------
git bash실행

git init

git config --global user.email "ho8566z@gmail.com"

git config --global user.name "ho8566z"

git config --list
#email, name 확인

------------------------------------------------------------------------
git status

git add .
#각각 추가할 때: git add (ex: doc.txt)

git commit -m "(ex)20260508 task complete"

------------------------------------------------------------------------
git log
#git commit 확인

github에 'sign in'하고 'repositories'개설할 것
#chrome에서 확인

git remote add origin https://~(github: repositories 코드)

git remote -v
#fetch, push 확인

git branch -M main
#master -> main 변경

git push origin main
#github에 업로드

------------------------------------------------------------------------
git clone https://~(github: repositories 코드) .
#github에서 '처음' 다운로드
#마지막에 "." 찍기

git pull origin main
#github에서 '다시' 다운로드

------------------------------------------------------------------------
------------------------------------------------------------------------
git checkout (특정commit번호)
#특정 commit으로 이동

git reflog
#사라진 log기록 전체 확인

git checkout master
#head -> master 관계 재설정

git reset --hard (특정commit번호)
#특정 commit으로 이동(관계 유지)

------------------------------------------------------------------------
git restore --staged doc.txt
#특정 파일, 디렉토리 '스테이지(add상태)'에서 add상태 이전으로

------------------------------------------------------------------------
------------------------------------------------------------------------
gitignore
#git 디렉토리에서 github에 업로드를 원치 않는 파일, 디렉토리 업로드를 막는 것
#새로 txt를 만들고 파일명을 ".gitignore"로 설정하고,
#새 txt에 '.gitignore'를 드래그 해서 실행하고, 원치 않는 파일명, 디렉토리명을 
#입력하면 완료
#-> git status, add, commit 진행하고, push진행하고, 
#repositories에서 진짜 없는지 확인할 것

------------------------------------------------------------------------
------------------------------------------------------------------------
------------------------------------------------------------------------
------------------------------------------------------------------------
MS VScode에서의 git bash실행

파일탐색기 --> 내pc --> 왼클릭: 속성 --> 고급 시스템 속성 --> 환경변수
--> 새로만들기: C:\Program Files\Git\cmd를 복사/붙여넣기 --> 재확인하기
--> 모든 관련 창 끄고, MS VScode 재실행해서 'git status'로 마무리 확인하기

------------------------------------------------------------------------
MS VScode에서의 git bash : add + commit 동시에 하는법

PS C:\lyh\python\python_ex> git commit -am "20260521 14:49"
--> git commit -am "~"

PS C:\lyh\python\python_ex> git add .; git commit -m "20260521 14:50"
--> git add .; git commit -m "~"

------------------------------------------------------------------------
MS VScode에서의 git bash : add + commit + push 동시에 하는법
PS C:\lyh\python\python_ex> git add .; git commit -m "20260521 14:54"; git push origin main;
--> git add .; git commit -m "~"; git push origin main;

------------------------------------------------------------------------
MS VScode에서 실행메뉴 3번째: git문양을 누르고, 원하는 commit메세지 치고,
commit버튼 누르고, Sync Changes누르면 push까지 완료


https://hoazzinews.tistory.com/210
--> 위의 주소 참고하기

------------------------------------------------------------------------
------------------------------------------------------------------------
------------------------------------------------------------------------
------------------------------------------------------------------------
#github 인증하는 방법

1. id, pw로 브라우저 인증 -> 취미 생활

2. HTTPS + Persnal access token -> 1번보다 약간 고급진 방법

3. SSH key -> 가장 안전하고, 안정적인 방법(1대의 컴퓨터만 사용가능)

------------------------------------------------------------------------
------------------------------------------------------------------------
# SSH key 방법

1. 제어판-> 사용자 계정-> window 자격 증명 관리자-> github 브라우저 인증 [제거]

2. '돋보기'에 'cmd' 검색, 열어서 "mkdir %USERPROFILE%\.ssh\git_keys"입력 및 enter

3. c드라이브-> 사용자(or, user)-> ".ssh" 및 기타 폴더 확인하기

4. 'cmd'에서 "ssh-keygen -t ed25519 -C "ho8566z@gmail.com"
 -f %USERPROFILE%\.ssh\git_keys\id_ed25519_main" 입력하기

5. .ssh-> git_keys에서 개인키, 공개키 확인하기

6. github-> user.settings-> [SSH and GPG keys]에 새로운 SSH key 등록하기
(cmd에서 "type %USERPROFILE%\.ssh\git_keys\id_ed25519_main.pub" 입력해서 
 SSH key 확인)

7. '돋보기'에 "services.msc"를 입력하고, 'openssh agent~'를 찾아서 '사용안함'을
'자동'으로 [선택]하고, [적용], [시작], [확인]하기

8. 'cmd'에서 "ssh-add %USERPROFILE%\.ssh\git_keys\id_ed25519_main" 입력하기

9. 등록 완료 확인하고, "ssh -T git@github.com" 입력하고, "yes"입력 및 enter

10. 연결 상태 확인하고 메세지 확인하기

11. 각각의 'Local Repository'에서 'git bash' 실행해서 github에 인식시키기

------------------------------------------------------------------------
------------------------------------------------------------------------
