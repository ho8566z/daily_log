#### N:1 관계

- user_member의 authority_no와 user_authority의 no는 N:1 (다대일) 관계이다.
- 의미
    - 여러 개의 user_member(N) 가 하나의 user_authority(1) 를 참조할 수 있다.
    - 즉, 여러 회원이 같은 권한(예: "USER", "ADMIN")을 공유한다.

```sql
- user_authority --------------------------------------------------------
**CREATE** **TABLE** user_authority(
no				**INT**				**AUTO_INCREMENT**,
role_name		**VARCHAR**(20)		**NOT** **NULL**,
**PRIMARY** **KEY**(no)
);
```

#### user_authority 테이블에서 role_name를 Database가 아닌, Application에서 권한을 설정하는 방법

- Lambda 표현식을 사용한 DataInitializer 코드

```java
@Configuration
public class DataInitializer {

    @Bean
    public CommandLineRunner initDatabase(AuthorityRepository authorityRepository) {

        returnargs -> {

            if(!authorityRepository.existsByAuthRoleName("PRE_USER")) {
                authorityRepository.save(
                        AuthorityEntity.builder()
                                .authRoleName("PRE_USER")
                                .build()
                );
            }

            if(!authorityRepository.existsByAuthRoleName("USER")) {
                authorityRepository.save(
                        AuthorityEntity.builder()
                                .authRoleName("USER")
                                .build()
                );
            }

        };

    }

}
```

- AuthorityEntity 코드

```java
package com.office.calendar.member.jpa;

import jakarta.persistence.*;
import lombok.AllArgsConstructor;
import lombok.Builder;
import lombok.Data;
import lombok.NoArgsConstructor;

@Entity
@Table(name = "USER_AUTHORITY", uniqueConstraints = {@UniqueConstraint(columnNames = "ROLE_NAME")})
@Data
@Builder
@NoArgsConstructor
@AllArgsConstructor
public class AuthorityEntity {

@Id
@Column(name = "NO")
@GeneratedValue(strategy = GenerationType.IDENTITY)
private byte authNo;

@Column(name = "ROLE_NAME", nullable = false, length = 20)
private String authRoleName;
}
```

- AuthorityRepository 코드

```java
package com.office.calendar.member.jpa;

import org.springframework.data.jpa.repository.JpaRepository;

public interface AuthorityRepository extends JpaRepository<AuthorityEntity, Byte> {

public boolean existsByAuthRoleName(String authRoleName);
}
```

---

# Spring Security Framework

## Spring Security Framework 란?

- Java 기반 애플리케이션(특히 Spring 프레임워크 기반)에서 인증(Authentication)과 인가(Authorization)를 담당하는 보안 프레임워크이다.

#### 기존 로그인 방식의 한계

- 세션을 이용한 로그인 방식
    - 사용자가 로그인에 성공하면 서버가 해당 사용자의 정보를 서버 메모리(세션 저장소) 에 저장하고, 클라이언트에게는 그 정보를 식별할 수 있는 세션 ID만 쿠키로 전달하는 방식이다.
- 보안 취약점
    - 보안 취약점 이란?
    - 보안 취약점 이란?
    - 보안 취약점 이란?
    - 보안 취약점 이란?
    - 보안 취약점 이란?
    - 보안 취약점 이란?
- 유지보수의 어려움
    - 유지보수의 어려움 이란?

#### Spring Security의 필요성

- 보안(Security)은 웹 애플리케이션의 필수 요소이다.
- Spring Boot에서 강력한 보안 프레임워크를 제공한다.
- 주요 기능
    - 개인정보 보호
    - 인증과 인가 기능 제공
    - 불법적인 접근 차단

---

## Spring Security의 주요 기능

#### 인증(Authentication)

- 인증(Authentication) 이란?

#### 인가(Authorization)

- 인가(Authorization) 이란?

#### 자동 보안 설정

- 자동 보안 설정 이란?

---

#### Spring Security 구조 흐름

---

#### build.gradle 코드

```
// Source: <https://mvnrepository.com/artifact/org.springframework.boot/spring-boot-starter-security>
implementation 'org.springframework.boot:spring-boot-starter-security'

// Source: <https://mvnrepository.com/artifact/org.thymeleaf.extras/thymeleaf-extras-springsecurity6>
implementation 'org.thymeleaf.extras:thymeleaf-extras-springsecurity6'
```

#### SecurityConfig.java 코드